import { css } from '@emotion/react'

export const globalStyles = css`
    html {
        font-family: sans-serif;
    }
    body {
        margin: 0;
    }
    html,
    body,
    #root {
        height: 100%;
        overflow: hidden;
    }
`
